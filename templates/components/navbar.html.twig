<nav class="fixed top-0 left-0 w-full z-50 bg-white/80 backdrop-blur-xl border-b border-gray-100 shadow-sm">

<div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">

            <div class="text-xl font-semibold tracking-tight text-gray-900">
                <a href="{{ path('app_index') }}#home" class="hover:text-blue-600 transition duration-300">
                    <i class="fas fa-code text-blue-600 mr-1"></i>
                    <span class="ml-2">Sarra Mseibi</span>
                </a>
            </div>


            <div class="hidden md:flex items-center space-x-8">

                {# SHOW ONLY FOR NON-ADMIN USERS #}
                {% if not is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_index') }}#about"
                       class="nav-link relative text-gray-700 font-medium hover:text-blue-600 transition duration-300 after:absolute after:left-0 after:-bottom-1 after:w-0 after:h-0.5 after:bg-blue-600 after:transition-all after:duration-300 hover:after:w-full">
                        About Me
                    </a>

                    <a href="{{ path('app_index') }}#projects"
                       class="nav-link text-gray-700 hover:text-blue-600 transition font-medium">
                        Projects
                    </a>

                    <a href="{{ path('app_index') }}#skills"
                       class="nav-link text-gray-700 hover:text-blue-600 transition font-medium">
                        Skills
                    </a>

                    <a href="{{ path('app_index') }}#contact"
                       class="nav-link text-gray-700 hover:text-blue-600 transition font-medium">
                        Contact
                    </a>
                {% endif %}

                {# OPTIONAL: ADMIN DASHBOARD LINK #}
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('admin_dashboard') }}"
                       class="nav-link text-blue-600 font-semibold">

                    </a>
                {% endif %}

            </div>



            <div class="hidden md:flex items-center space-x-4">
                <div class="hidden md:flex items-center space-x-4">
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}"
                           class="px-6 py-3 border-2 border-blue-600 text-blue-600 rounded-lg font-medium hover:bg-blue-50 transition">
                            Logout
                        </a>
                    {% else %}
                        <a href="{{ path('app_login') }}"
                           class="px-6 py-2 rounded-xl border border-blue-600/80 text-blue-600 font-medium hover:bg-blue-50 transition-all duration-300">

                           Sign In
                        </a>

                        <a href="{{ path('app_register') }}"
                           class="px-6 py-2 rounded-xl border border-blue-600/80 text-blue-600 font-medium hover:bg-blue-50 transition-all duration-300"
                        >
                            Sign Up
                        </a>
                    {% endif %}
                </div>

            </div>


            <button
                data-action="click->navbar#toggleMobile"
                class="md:hidden text-gray-700 hover:text-blue-600 focus:outline-none">
                <i class="fas fa-bars text-2xl"></i>
            </button>
        </div>



    </div>
</nav>
